# Publicar V6.7

1. Subir todo el contenido a la raíz del repositorio.
2. Commit sugerido: `Mi Solar V6.7 - casas reales Arrayán y Puerto Montt`.
3. Netlify: build `npm run build`, publish `dist`, functions `netlify/functions`.
4. Las escenas están en `src/assets/scenes` y serán incluidas automáticamente en el build.
