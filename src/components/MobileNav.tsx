import { BarChart3,BatteryCharging,CircleDollarSign,Clock3,Droplets,Home,PlugZap,CloudSun,Users,WalletCards } from 'lucide-react';import type { PageKey } from '../types';
const items:[PageKey,string,any][]=[['home','Inicio',Home],['charts','Gráficos',BarChart3],['solar','Radiación',CloudSun],['costs','Cuenta Enel',CircleDollarSign],['equipment','Equipos',BatteryCharging],['programming','Programar',Clock3],['integrations','Domótica',PlugZap],['water','Cuenta Aguas Cordillera',Droplets],['family','Familia',WalletCards],['users','Usuarios',Users]];
export default function MobileNav({page,setPage,waterEnabled,showUsers,showFamily,allowedMenus}:{page:PageKey;setPage:(p:PageKey)=>void;waterEnabled:boolean;showUsers:boolean;showFamily:boolean;allowedMenus?:Partial<Record<PageKey,boolean>>}){const visible=items.filter(([key])=>allowedMenus?.[key]!==false&&(key!=='water'||waterEnabled)&&(key!=='users'||showUsers)&&(key!=='family'||showFamily));return <nav className="mobile-nav">{visible.map(([k,l,I])=><button key={k} className={page===k?'active':''} onClick={()=>setPage(k)}><I size={20}/><small>{l}</small></button>)}</nav>}
