import { CalendarDays,ChevronLeft,ChevronRight } from 'lucide-react';
const shift=(date:string,n:number)=>{const [y,m,d]=date.split('-').map(Number);return new Date(Date.UTC(y,m-1,d+n)).toISOString().slice(0,10)};
export default function HomeDateNavigator({value,max,onChange,loading=false}:{value:string;max:string;onChange:(v:string)=>void;loading?:boolean}){const today=value===max;return <section className="panel home-date-navigator"><div><CalendarDays/><span><strong>Día mostrado</strong><small>{today?'Datos de hoy en tiempo real':'Consulta histórica desde Mi Solar'}</small></span></div><div className="home-date-controls"><button onClick={()=>onChange(shift(value,-1))}><ChevronLeft/></button><input type="date" value={value} max={max} onChange={e=>e.target.value&&onChange(e.target.value)}/><button disabled={today} onClick={()=>onChange(shift(value,1))}><ChevronRight/></button>{!today&&<button className="today-button" onClick={()=>onChange(max)}>Volver a hoy</button>}</div>{loading&&<small>Cargando…</small>}</section>}
