import { useEffect, useRef } from 'react';
import * as echarts from 'echarts';
export default function EChart({option,className='chart'}:{option:Record<string,unknown>;className?:string}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const chart=echarts.init(ref.current);const enhanced={aria:{enabled:true},toolbox:{show:true,right:8,top:4,iconStyle:{borderColor:'#8ea6ad'},emphasis:{iconStyle:{borderColor:'#ffffff'}},feature:{dataZoom:{title:{zoom:'Ampliar',back:'Deshacer ampliación'}},restore:{title:'Restablecer'},saveAsImage:{title:'Guardar imagen',pixelRatio:2}}},...option};chart.setOption(enhanced as echarts.EChartsOption);const ro=new ResizeObserver(()=>chart.resize());ro.observe(ref.current);return()=>{ro.disconnect();chart.dispose()}},[option]);return <div ref={ref} className={className}/>}
